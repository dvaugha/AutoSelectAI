---
description: Auto-sync and build workflow for Alpha-X project
---

// turbo-all
1. Stage all changes: `git add .`
2. Commit with a descriptive message: `git commit -m "..."`
3. Push to main: `git push origin main`
