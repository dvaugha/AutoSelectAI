---
description: Automatically commit and push changes to the main branch
---

1. // turbo
   Add all changed files to git
   `git add .`

2. // turbo
   Commit changes with a descriptive message
   `git commit -m "Auto-sync: update project features"`

3. // turbo
   Push changes to the main branch
   `git push origin main`
